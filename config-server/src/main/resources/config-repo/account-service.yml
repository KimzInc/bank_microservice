server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/account_db
    username: kimzinc
    password: ILoveYouSo@1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# MUST exactly match jwt.secret in config-repo/auth-service.yml - employee-service
# doesn't issue tokens, but it validates them independently (Pattern B), which only
# works if both services sign/verify with the identical shared secret.
jwt:
  secret: 1a0267c655cd203405b30d90a2a121e5ea0290c45e8834837e7da2c1ce077ce9
  expiration-ms: 3600000
