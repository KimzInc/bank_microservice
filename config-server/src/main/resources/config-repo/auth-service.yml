# Fetched automatically by auth-service on startup because its
# spring.application.name = auth-service matches this file name.
server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db
    username: kimzinc
    password: ILoveYouSo@1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# Shared secret used to sign/verify JWTs (Pattern A/B from the architecture doc).
# In real life this comes from a vault, never checked into config like this.
jwt:
  secret: change-this-to-a-long-random-string-before-you-ever-deploy-anywhere
  expiration-ms: 3600000
