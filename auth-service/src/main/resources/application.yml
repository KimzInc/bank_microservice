spring:
  application:
    name: auth-service
  config:
    # Real settings (port, datasource, jwt secret) live in
    # config-server/src/main/resources/config-repo/auth-service.yml
    import: optional:configserver:http://localhost:8888
